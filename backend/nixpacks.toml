[phases.setup]
nixPkgs = ["python311", "ffmpeg"]

[phases.install]
cmds = ["python -m venv /app/.venv", "/app/.venv/bin/pip install -r requirements.txt"]

[start]
cmd = "/app/.venv/bin/gunicorn app:app --bind 0.0.0.0:$PORT --timeout 180 --workers 1"